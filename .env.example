# =============================================================================
# DATABRICKS AUTHENTICATION
# =============================================================================
# Use a profile from ~/.databrickscfg instead of explicit credentials
DATABRICKS_CONFIG_PROFILE=DEFAULT

# Workspace URL (used for building MCP server URLs)
DATABRICKS_WORKSPACE_URL=https://your-workspace.cloud.databricks.com

# Alternative: Explicit credentials (not recommended)
# DATABRICKS_HOST=https://your-workspace.cloud.databricks.com
# DATABRICKS_TOKEN=your_databricks_token

# =============================================================================
# MODEL CONFIGURATION
# =============================================================================
DATABRICKS_MODEL_NAME=databricks-meta-llama-3-1-70b-instruct

# =============================================================================
# MCP SERVERS CONFIGURATION
# =============================================================================
# Control which MCP servers the agent connects to for tools

# Databricks Managed MCP Server Paths (comma-separated, without workspace URL)
# The workspace URL will be automatically prepended from DATABRICKS_WORKSPACE_URL
# Documentation: https://docs.databricks.com/aws/en/generative-ai/mcp/managed-mcp
#
# Available server types:
#   - System AI functions: /api/2.0/mcp/functions/system/ai
#   - Unity Catalog functions: /api/2.0/mcp/functions/{catalog}/{schema}
#   - Genie spaces: /api/2.0/mcp/genie/{space_id}
#   - Vector Search: /api/2.0/mcp/vector-search/{endpoint_name}
#
# Examples:
#   System AI + Genie space:
#   DATABRICKS_MCP_SERVERS=/api/2.0/mcp/functions/system/ai,/api/2.0/mcp/genie/01ef9a8b123456789abcdef
#
#   System AI + Vector Search:
#   DATABRICKS_MCP_SERVERS=/api/2.0/mcp/functions/system/ai,/api/2.0/mcp/vector-search/my_endpoint
#
#   Multiple servers:
#   DATABRICKS_MCP_SERVERS=/api/2.0/mcp/functions/system/ai,/api/2.0/mcp/genie/space_123,/api/2.0/mcp/vector-search/my_endpoint
DATABRICKS_MCP_SERVERS=/api/2.0/mcp/functions/system/ai

# External MCP Servers (comma-separated, full URLs)
# Self-hosted or third-party MCP servers outside Databricks
# Examples:
#   - Custom hosted: https://custom-mcp-server.company.com
#   - Third-party: https://external-service.com/mcp
EXTERNAL_MCP_SERVERS=

# =============================================================================
# MLFLOW TRACING CONFIGURATION
# =============================================================================
# Enable MLflow tracing for agent interactions
MLFLOW_ENABLE_TRACING=true

# MLflow experiment name for tracking agent runs
MLFLOW_EXPERIMENT_NAME=/Users/your-username/langgraph-mcp-agent

# Optional: MLflow tracking URI (defaults to Databricks workspace)
# MLFLOW_TRACKING_URI=databricks

# Optional: Timeout for flushing traces on exit (in seconds, default: 30)
# Increase if you have many traces or slow network
# MLFLOW_EXPORT_TIMEOUT=30
